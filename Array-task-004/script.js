// Use the for loop on the above array to print elements in the odd index.

const myArray = [10,13,50,1,6,8,30];

for(let i = 0; i<myArray.length;i++){
  console.log(`Element of index:${i} is ${myArray[i]}`);
}