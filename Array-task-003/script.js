//Create an array of 6 elements using the Array literals and access the fourth element in the array using its length property.
const arrayLiterals = [1,2,3,4,5,6,7];

const fourthArr = arrayLiterals[arrayLiterals.length-3];


console.log(fourthArr);
console.log(arrayLiterals);
console.table(arrayLiterals);